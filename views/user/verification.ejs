<%- include("../partials/header", { title:" Verification "}) %>


<div class="container d-flex justify-content-center align-items-center" style="min-height:80vh;">

    <div class="card shadow-sm p-4 text-center" style="max-width:420px; width:100%;">

        <div class="mb-3">
            <div class="bg-light rounded-circle mx-auto d-flex align-items-center justify-content-center"
                 style="width:60px; height:60px;">
                📱
            </div>
        </div>

        <h5>Enter Verification Code</h5>
        <p class="text-muted small">
            We’ve sent a 6-digit code to your phone number ending in
            <strong><%= data.phoneEnding %></strong>
        </p>

        <div class="d-flex justify-content-between mb-4">
            <input class="form-control otp-box" maxlength="1">
            <input class="form-control otp-box" maxlength="1">
            <input class="form-control otp-box" maxlength="1">
            <input class="form-control otp-box" maxlength="1">
            <input class="form-control otp-box" maxlength="1">
            <input class="form-control otp-box" maxlength="1">
        </div>

        <button class="btn btn-dark w-100 mb-3">Verify Code</button>

        <p class="small text-muted mb-2">
            Code expires in <strong><%= data.expiry %></strong>
        </p>

        <p class="small mb-3">
            Didn't receive the code?
        </p>

        <div class="d-flex justify-content-center gap-2 small mb-3">
            <a href="#" class="text-decoration-none">Resend via SMS</a>
            <span>•</span>
            <a href="#" class="text-decoration-none">Call me instead</a>
        </div>

        <p class="small">
            <a href="/signin" class="text-decoration-none">← Back to login</a>
        </p>

    </div>
</div>

<%- include("../partials/footer") %>
